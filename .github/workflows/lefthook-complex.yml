name: Lefthook Complex

on:
  workflow_dispatch:
    inputs:
      tier:
        description: "Heavy lefthook tier to run on the whole tree"
        type: choice
        options: [complex, release]
        default: complex

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lefthook-complex:
    uses: ffreis-org/ffreis-workflows-general/.github/workflows/general-lefthook-complex.yml@287f836a726a35af37bd24561fc687f66eaeb92f # v1.7.0
    with:
      working-directory: "."
      tier: ${{ inputs.tier }}
      setup-go: false
      install-golangci-lint: false
      install-govulncheck: false
      lefthook-bootstrap-command: "make lefthook-bootstrap"
      lefthook-bin: ".bin/lefthook"
